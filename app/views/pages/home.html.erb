
<div class="hero text-center">
  <% if user_signed_in? %>
  
  <% else %>
  <!--<h2>MyTMI</h2> -->
  <%= image_tag("myTMI_logo_small.png") %>
  
  <% end %>
  
</div>

<div class="row">
  <% if user_signed_in? %>
    <div class="col-md-4  col-md-offset-4 text-center">
      <div class="well">
        <!-- <h2 class="text-center">Hello!</h2> 
        <% if current_user.profile %>
          <%= link_to "Profile", edit_user_profile_path(current_user), class: 'btn btn-success btn-lg btn-block' %>
        <% else %>
          <%= link_to "Create your profile", new_user_profile_path(current_user), class: 'btn btn-success btn-lg btn-block' %>
        <% end %>
      -->
        
        <%= link_to "Diary", diary_path, class: 'btn btn-success btn-lg btn-block' %>
        <%= link_to "Bodymap", bodymap_path, class: 'btn btn-success btn-lg btn-block' %>
        <%= link_to "History", history_path, class: 'btn btn-success btn-lg btn-block' %>
        <%= link_to "Forums", forums_path, class: 'btn btn-success btn-lg btn-block' %>
        <!-- 
        <%= link_to "Search", search_path, class: 'btn btn-success btn-lg btn-block' %>
        -->
      </div>
    </div>
  <% else %>
    
    <div class="col-md-4  col-md-offset-4 text-center">
        <%= link_to "Sign up for free", new_user_registration_path, class: 'btn btn-lg btn-success' %>
    
    </div>
    
  <% end %>

</div>