
<div class="hero text-center">
  <h2>MyTMI</h2>
  
  
</div>

<div class="row">
  <% if user_signed_in? %>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Your profile</h2>
        <% if current_user.profile %>
          <%= link_to "Edit your profile", edit_user_profile_path(current_user), class: 'btn btn-primary btn-lg btn-block' %>
          <%= link_to "View your profile", user_path(current_user), class: 'btn btn-primary btn-lg btn-block' %>
        <% else %>
          <%= link_to "Create your profile", new_user_profile_path(current_user), class: 'btn btn-primary btn-lg btn-block' %>
        <% end %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center"></h2>
        <p></p>
        <%= link_to "Community", users_path, class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
  <% else %>
    
    <div class="col-md-4  col-md-offset-4 text-center">
        <%= link_to "Sign up for free", new_user_registration_path, class: 'btn btn-lg btn-success' %>
    
    </div>
    
  <% end %>

</div>